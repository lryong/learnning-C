# include <stdio.h>

int main()
{
    int *ptr = NULL;
    printf("ptr's value is %p\n",ptr);
    return 0;
    if (ptr)
    {
        printf("ptr's is nil\n");
    } else {
        printf("ptr's is nil\n");
    }
}
